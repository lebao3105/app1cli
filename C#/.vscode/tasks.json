{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dotnet_debug",
            "type": "shell",
            "command": " rm -rf bin obj && rm -rf maths/{bin,obj} && rm -rf utils/{bin,obj} && dotnet build && rm -rf bin obj && rm -rf maths/{bin,obj} && rm -rf utils/{bin,obj} && dotnet run",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}